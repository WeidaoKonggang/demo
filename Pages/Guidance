import React from 'react';
import { useNavigate } from 'react-router-dom';
import { createPageUrl } from '@/utils';
import { Button } from '@/components/ui/button';
import { ArrowLeft, Lightbulb, Camera, Map, Home as HomeIcon, BookOpen } from 'lucide-react';

export default function Guidance() {
  const navigate = useNavigate();

  const guides = [
    {
      icon: Camera,
      title: "Scanning Artifacts",
      description: "Use your camera to scan QR codes or take photos of artifacts. The AI will identify and provide detailed information."
    },
    {
      icon: Map,
      title: "Virtual Tours",
      description: "Explore museum galleries virtually. Follow the AI companion's recommended routes for the best experience."
    },
    {
      icon: HomeIcon,
      title: "Decorating Your House",
      description: "Unlock 2D artifact images by completing quests. Use them to decorate your companion's house and create a memory palace."
    },
    {
      icon: BookOpen,
      title: "Collection Progress",
      description: "Track your learning journey in the Collection Book. Unlock more artifacts by exploring and completing quests."
    }
  ];

  return (
    <div className="min-h-screen bg-[#0f0f0f] relative">
      {/* Background matching Home */}
      <div 
        className="absolute inset-0 bg-cover bg-center opacity-8"
        style={{
          backgroundImage: "url('https://images.unsplash.com/photo-1578301978018-3005759f48f7?w=1600')"
        }}
      />

      {/* Gradient overlay matching Home */}
      <div className="absolute inset-0 bg-gradient-to-b from-[#0f0f0f]/80 via-[#0f0f0f]/50 to-[#0f0f0f]/90" />

      <div className="relative z-10 min-h-screen pt-20">
        {/* Header */}
        <div className="px-6 mb-6 text-center">
          <h1 className="text-white font-semibold text-xl">Guidance</h1>
        </div>

        <div className="px-6 space-y-6">
          {/* Introduction */}
          <div className="backdrop-blur-2xl bg-white/[0.02] border border-white/5 rounded-2xl p-6 shadow-xl">
            <div className="flex items-start gap-3">
              <div className="w-10 h-10 rounded-full bg-amber-400/10 flex items-center justify-center flex-shrink-0">
                <Lightbulb className="w-5 h-5 text-amber-400" />
              </div>
              <div>
                <p className="text-amber-400 font-medium mb-2">Welcome to TimeLens!</p>
                <p className="text-white leading-relaxed">
                  Your AI companion for exploring museums and unlocking the secrets of civilizations. 
                  Here's how to get the most out of your journey through time.
                </p>
              </div>
            </div>
          </div>

          {/* Guide cards */}
          {guides.map((guide, index) => (
            <div key={index} className="backdrop-blur-2xl bg-white/[0.02] border border-white/5 rounded-2xl p-6 shadow-xl">
              <div className="flex items-start gap-4">
                <div className="w-12 h-12 rounded-xl bg-amber-400/10 flex items-center justify-center flex-shrink-0">
                  <guide.icon className="w-6 h-6 text-amber-400" />
                </div>
                <div>
                  <h3 className="text-white font-semibold mb-2">{guide.title}</h3>
                  <p className="text-gray-300 leading-relaxed">{guide.description}</p>
                </div>
              </div>
            </div>
          ))}

          {/* Tips section */}
          <div className="backdrop-blur-2xl bg-white/[0.02] border border-white/5 rounded-2xl p-6 shadow-xl">
            <h3 className="text-white font-semibold mb-4">Pro Tips</h3>
            <ul className="space-y-3">
              <li className="flex items-start gap-3">
                <div className="w-1.5 h-1.5 rounded-full bg-amber-400 mt-2 flex-shrink-0" />
                <p className="text-gray-300">Complete quests to earn XP and unlock more artifacts</p>
              </li>
              <li className="flex items-start gap-3">
                <div className="w-1.5 h-1.5 rounded-full bg-amber-400 mt-2 flex-shrink-0" />
                <p className="text-gray-300">Share your discoveries in the Community to inspire others</p>
              </li>
              <li className="flex items-start gap-3">
                <div className="w-1.5 h-1.5 rounded-full bg-amber-400 mt-2 flex-shrink-0" />
                <p className="text-gray-300">Watch the AI companion's hints carefully during quests</p>
              </li>
              <li className="flex items-start gap-3">
                <div className="w-1.5 h-1.5 rounded-full bg-amber-400 mt-2 flex-shrink-0" />
                <p className="text-gray-300">Revisit your House to review what you've learned</p>
              </li>
            </ul>
          </div>

          {/* Reward info */}
          <div className="backdrop-blur-2xl bg-green-500/10 border border-green-400/30 rounded-2xl p-6 shadow-xl">
            <div className="text-center">
              <div className="text-4xl mb-3">🎉</div>
              <h3 className="text-white font-semibold mb-2">Quest Completed!</h3>
              <p className="text-gray-300 mb-4">
                You've earned the Ancient Greek Vase 2D image and 50 XP points!
              </p>
              <div className="inline-block px-4 py-2 bg-green-500/20 rounded-full">
                <span className="text-green-400 font-semibold">+50 XP</span>
              </div>
            </div>
          </div>

          {/* Start Tour button */}
          <Button
            onClick={() => navigate(createPageUrl('Tour'))}
            className="w-full bg-amber-400 hover:bg-amber-500 text-black font-semibold py-6 rounded-xl text-lg shadow-lg shadow-amber-400/20 hover:translate-y-[-4px] transition-all duration-200"
          >
            Start Tour
          </Button>
        </div>
      </div>
    </div>
  );
}
